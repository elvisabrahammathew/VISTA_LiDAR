[package]
name = "vista-edge"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Cross-platform edge sensor capture and processing runtime"
license = "MIT"

[lib]
name = "vista_edge"
path = "lib.rs"

[[bin]]
name = "vista_edge"
path = "main.rs"

[dependencies]

[lints.rust]
# Unsafe native calls are allowed only inside reviewed OS/SDK adapters.
unsafe_code = "warn"
