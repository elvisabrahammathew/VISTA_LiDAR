[package]
name = "vista-lidar"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Cross-platform LiDAR capture, decoding, and point-cloud preprocessing"
license = "MIT"

[lib]
name = "vista_lidar"
path = "lib.rs"

[[bin]]
name = "quanergy-m8-capture"
path = "main.rs"

[dependencies]

[lints.rust]
# Native SDK calls are isolated and explicitly allowed only in the RealSense driver.
unsafe_code = "deny"
